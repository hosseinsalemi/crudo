/**
 * @crudo/nest — NestJS binding for Crudo.
 *
 * Milestone A scaffold: architecture in Phase 11, implementation in
 * Phase 12 (`CrudoModule`, `@Crud` decorator, registry-driven route
 * generation, exception filter, Swagger). This package augments
 * `OperationMetadata` with its `routes` key — see
 * packages/docs/architecture/03-core-contracts-and-type-system.md.
 * `@nestjs/*` will be peerDependencies, declared in Phase 19.
 */
export {};
