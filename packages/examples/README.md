# examples

Home of the Phase 18 reference application (a project-management API:
`User`, `Project`, `Task`, `Comment`, `Tag`), grown from the Milestone B
checkpoint app. Empty until then.

The reference app consumes only public package APIs — if it ever needs a
deep import, that is an API-surface bug in the package, not the app.
